# Meetup Planner — чеклист GHG8

Преемник `CHECKLIST_GHG7.md` (архив релиза P0–P10 + **все закрытые блоки GHG8**,
см. там секцию «ЗАКРЫТО GHG8»). Здесь — **только то, что предстоит сделать**.
Детали закрытых задач — `git log --grep "GHG8"`.

Single source of truth: `C:\Users\fa1nt\meetup-planner-main` (монорепо
`backend/` + `frontend/`). После правок `backend/` —
`cp backend/* → C:\Users\fa1nt\meetup-planner-backend\` (отдельный git remote →
HF Space). Push в HF/GitHub делает ассистент (git PAT) ИЛИ пользователь —
фиксируется в каждом sync-подэтапе.
⚠️ Push в GitHub через credential.helper=manager ВИСНЕТ (молча, >5 мин) —
пушить с PAT в URL:
`git push https://x-access-token:<PAT>@github.com/kickmesc-dotcom/meetup-planner.git main`.

## Легенда статусов
- `[ ]` — не начато · `[~]` — в работе (обрыв/ожидание) · `[x]` — закрыто

## Правила работы с чеклистом
1. Этапы разбиты на подэтапы `PX.Y` / `PX.Y.a`.
2. После закрытия подэтапа — `[x]` + однострочная пометка
   `(YYYY-MM-DD) <что сделано> — <ключевой файл/коммит>`.
3. Sync `backend/` → `meetup-planner-backend/` — отдельный подэтап с галкой.
4. Обрыв → `[~]` с пометкой, на каком файле/шаге остановились.
5. Investigate-пункты (`INV-N`) не «фиксят», а отвечают на вопрос и порождают
   follow-up подэтапы; закрываются выводом + ссылкой на них.
6. **Закрытый блок целиком → переносим однострочником в `CHECKLIST_GHG7.md`
   («ЗАКРЫТО GHG8») и удаляем отсюда.** Рабочий файл держим компактным —
   только открытое, чтобы не таскать готовый код в контекст каждой сессии.

Состояние прода (2026-06-12): HF backend на `9d5c702` (HOTFIX BOT_API_SERVER).
⚠️ Открыт ручной шаг P6.1.b — сидинг 6 персоналий через админку «🎭 Персоналии»
(тексты в Neon, не в git). ⚠️ env `HF_TOKEN` (write-токен HF для рестарта Space,
P14) — добавить руками в секреты Space; без него кнопка рестарта дизейблится,
остальное работает. Q-NET.b добавил опц. env-тюнеры пула/таймаутов
(`BOT_KEEPALIVE_TIMEOUT`/`BOT_TOTAL_TIMEOUT`/`BOT_SOCK_CONNECT_TIMEOUT`/
`BOT_FORCE_CLOSE`) — все с дефолтами, руками выставлять НЕ обязательно. Прочее —
в `admin_config`.

⚠️ **Egress HF→Telegram (инцидент 11–12.06).** Прямой исходящий из HF Space в
`api.telegram.org` не проходит (timeout) — вход (webhook) при этом жив. Бот
«слепой»: апдейты получает, ответить не может. Лечится env `BOT_API_SERVER` =
URL cloudflare-воркера-реверс-прокси (worker переписывает host→api.telegram.org).
Прод сейчас: `BOT_API_SERVER=https://telegram-proxy.kickmesc.workers.dev`
(выставлен в variables Space). Пусто = direct (мёртвый канал). Запасной воркер —
`telegram-proxy2.esw1337.workers.dev`. Оба проверены живыми (getMe/sendMessage
ok). См. [[incident_meetup_planner_sock_connect_blind]] (это ДРУГОЙ, более ранний
слой того же «бот молчит» — sock_connect=8, уже починен).

---

## ОТКРЫТО

### F. Прод-фидбек после теста 10.06.2026
Источник — пользователь, дословно ниже. Низкий-средний приоритет, UI-полировка.

> 1. Если зайти в профиль, аватарка должна быть крупнее и по центру. Ниже
>    сводная инфа, типа как вы были лохом столько раз, чуханом столько раз.
>    Дальше едет кнопка топы. Дальше история, в истории главный смысл это видеть
>    не только участник+дата, но и причину.
> 2. Описание приветственного попапа и тогл нужно уместить на одной строчке,
>    это не такая важная фича чтобы занимать четверть экрана.
> 3. Формат участника тоже как настройка в одной строке.
Еще добавлено:
4. Аватарки перестали обновляться при ручном нажатии (как будто прод опять залип и висит, это поведение сохраняется уже пару недель)
5. Однажды я выставил ручные слоты времени и попытался нажать автоподбор, а он просто не дал мне ничего выбрать, поскольку было меньше трех вариантов, видимо из-за того что они оверлапились при виде
20:00 - 00:00, 19:00 - 23:00, 18:00 - 22:00, 17:00 - 21:00.
6. В очередной раз не сработал лох дня и просто исчез из очереди задач.
7. Постоянно путаю Запланированные задачи и Расписание задач. Кнопку расписание задач надо переименовать в Настройки задач
8. Для редактора персоналий мне нужна развернутая инструкция как его использовать и настраивать. Я изнаально планировал прописать характеры каждого участника и иметь возможность их дополнять, не только паттернами и примерами фраз, а в целом характерами, а за генерацию текста отвечало бы некое подобие LLM-ки. Здесь вообще не понятно как использовать, будет просто автовыплевыватель заготовленных фраз без какого либо рандома? Скука. 
9. При выборе версии Типажи я полез смотреть какие прописаны, там везде пусто, возвращаюсь обратно и выбрана нарезка. Надеюсь это преднамеренно.
10. Пресеты времени я уже указал выше наиболее адекватные, но при ручном изменении эти варианты просто исчезают из подставления в опросник без возможности начать опрос.
11. Немного неудобно что для каждого режима лох\чухан\реакты все подставлялки фраз лежат в отдельном меню. При этом это логично что они лежат в соотвествтвующих разделах, но хотелось бы по аналогии с интервалами иметь место где это все продублировано и собрано в кучу, можно было бы на лету глянуть сразу все фразы для всех режимов и даже свапнуть некоторые из них (кнопка рядом с фразой - удалить, отредактировать, переместить например из лоха в раздел чухан)
11.1 Еще я настолько много уже через интерфейс админки добавил клевых и смешных новых причин, удалив старые и несмешные, при этом не сохранив их в код\бд. (Это в принципе практически невозможно сделать когда у тебя по +100 позиций в каждом разделе). Будет обидно если они просто потеряются при следующем апдейте. Нужно что-то придумать чтобы я мог сохранить слепок текущей базы всех причин\реакций для всех режимов.
11.2. Там же, в реактах
12. В сводной истории в чухане отсутствует причина. Кстати, когда анонсируется чухан он просто пишет причину без приставки "Причина" иногда это конфузно фыглядит.
13. Реакции бота - здесь суперконфузно подписаны все пункты меню "ответ на реплай ответа" - что, я даже сам потерял связь и понимание для чего эти тогглеры. Нужно четкое разделение:
а) Бот всегда отвечает на упоминание через @ (вкл\выкл, по умолчанию вкл)
а здесь уже идет переключение между двумя разными функциями
б) Бот всегда отвечает на ВСЕ reply (если процитировать любое его сообщение из чата, включая анонсы, опросы, цитаты и т.д.)
в) бот отвечает на ВСЕ reply кроме ответов пользователей на сгенерированную цитату. Это сделано чтобы типы могли прокомментировать особо удачную шизо-цитату, но это не превращалось бы в переписку с пастой. (по умолчанию выбран этот вариант)
14. Тик напоминаний, авторебут, перепрооверка отправленности лоха\чухана (которая к слову ни разу не выполнила свою работу) - это все сейчас выставлено со слишком частыми интервалами и просто спамит пакетами наш беззащитный бесплатный сервак. Самое тупое что тик напоминаний я не могу даже сделать реже чем 120 мин. По хорошему такая вещь должна чекаться раз часов эдак в 6.
15. По системе реактов есть интерфейс добавления кастомных эмодзи реакций, что выглядело логично. Но потом я добавил туда много других вариантов и уже только потом вспомнил что не далеко каждое эмодзи можно выбирать как реакцию к посту. Здесь нужно либо изначально предлагать только те варианты которые 100% доступны как реакция, либо делать какую-то проверку на соответствие и отсекать любое подставленное юзером значение которое нельзя поставить в виде реакции.
16. Из-за того что статус бота залип, я весь день не могу принудительно прореактить на мем с кнопки (уже даже и не актуально), пишет что сообщение отправлено, но бот сегодня вообще ничего не присылал. А подборки он по прежнему криво и не всегда корректно распознает, в большинстве случаев просто не нашел. Скопировал лог в файл: incident10-06.txt
17. Лох дня накануне начал крутиться по два раза в день, хотя это не было задумано, но сегодня опять перестал срабатывать (из-за залипшего статуса), он пропал из планировщика задач, я зашел в блок настроек интервалов чтобы включить его заново и охренел от того как тупо и неочевидно все подписано. N - Каждые несколько часов, что вообще за бред. Мне нужны четкие и понятные режимы на выбор:
а) Есть предзаданный интервал привязанный к времени суток (сейчас 07 - 23), мы указываем сколько раз в течение этого интервала срабатывает автолох. Если 0 то 0, если 1, то 1, если 4 то 4. По клику принять программа сама распределяет время. Главное ограничить програмно чтобы не более 12 раз в интервале. 
б) Можно как с чуханом сделать просто ежедневный вариант, опять же в интервале, но например отключить по понедельникам. Настроить на один раз в день, с существующим интервалом 07 - 23 и выбрать как режим по умолчанию.
в) Выбрать конкретное время под каждый день недели. (Напр. в понедельник в 13:00 а в пятницу в 20:00)
18. Если в день в который назначен чухан или лох не было соответствующего поста в чатике, нужен некий блок алертов чтобы зашедшему в админку лишний раз это попало на глаза, можно там же где прокси закинуть где-то по сосетству. Там же сделать кнопку принудительного перепрогона (без алерта в чат что это было вручную) или возможностью в один клик запланировать в виде новой таски в течение ближайшего времени.
19. Я хочу полностью удалить файл checklist_ghg7, нужно убедиться что в нем либо закрыты все задачи либо перенесены в GHG8. Разработка должна стартовать с обращения к единому чеклисту без необходимости каждый раз заново лопатить все контексты.
20. А еще есть одна существенная штука. Я всей душой ненавижу интерфейсы где ты точно помнишь положение нужной кнопки, заходишь и желаешь тыкнуть, а он еще не прогрузил нужный тебе пункт, и к тому моменту когда элементы наконец прогрузились ты тыкаешь пальцем, а оно за секунду до этого прыгнуло из-за того что подгрузились еще пункты меню и сместили предыдущие, а ты в итоге кликнул вообще не туда и теперь в лучшем случае выходишь обратно и ждешь новой прогрузки, а в худшем - ты только что инициировал какой-то критически важный функционал в админке по типу рестартнуть спейс. Я больше за то чтобы приложение из коробки понимало сколько где кнопок и где они будут размещены, а уж раз так необходимо время на прогрузку, можно на их месте оставлять неактивные плейсхолдеры на те полсекунды пока там не прорисуется пункт меню, зато все всегда будет на местах и никаких прыгающих элементов. Важно это не критичный баг и он один из последних по приоритетности, более того, я готов применять решение только если оно относительно нетяжелое в плане кода и переписывания функционала. Если это будет массивный реворк на 60% всего дерева дизайна, то отложим как нереализованное на будущее.

> ⚠️ Удалён блок «инцидент 10–12.06» (бывшие п.21–25 + «добавлено12.06.2026»):
> забитый канал, cloudflare-прокси, миграция на amvera, новость про блокировку
> webhook-ботов в МСК_0. Первопричина — **внешняя РКН-блокировка вебхуков, вне
> нашего контроля**; от нас фиксов не требуется. Требования из той заметки уже
> закрыты в коде: `sock_connect=8` откатан (HOTFIX 2026-06-12 в `dispatcher.py`,
> плоский `total`), а `/api/worm/history` — не поллинг, а запрос по открытию
> попапа `🪱` (`enabled: open`), нагрузки на Neon не создаёт. Антиспам
> планировщика живёт в блоке **G**. Сырые заметки — `git log --grep "GHG8"`.

- [x] **F-media-fix. Медиа-реакции: «реагирует на 100% вне зависимости от
  настроек».** (2026-06-14) Диагноз: режимы `chance`/`wait_then_chance` гнали
  СЕРИЮ из 10 тиков с независимым роллом на каждом — кумулятивно при «шансе
  10–50%» бот срабатывал с вероятностью ~98% (просто растянуто во времени, потому
  и «с задержкой»). Плюс `chance` и `wait_then_chance` вели себя идентично.
  Решение (согласовано с пользователем): **один честный ролл на мем** + разделить
  режимы. `services/media_reactions.py` — удалены `WAIT_TICKS_MIN`/`tick_chance`,
  добавлены `DEFAULT_WAIT_WINDOW_MIN=15`/`WAIT_WINDOW_MIN_BOUNDS=(1,360)`/
  `clamp_wait_window`. `admin_config.py` — два ключа `chance_base_pct`/
  `chance_max_pct` → единый `chance_pct` (default 30) + `wait_window_min`
  (default 15). `handlers/media_reactions.py` — `_reaction_series` →
  `_wait_then_react` (одно ожидание): `chance`=ролл→реакция сразу,
  `wait_then_chance`=ролл→грейс-окно→реакция если люди промолчали. Фронт
  (`api/admin.ts`, `MediaReactionsScreen.tsx`) — одно поле «Шанс реакции» +
  «Грейс-окно, мин» (только для wait_then_chance), новые подписи режимов. Тесты
  переписаны под single-roll (+ проба честности 30%≈30%), весь сьют **385 passed**;
  TS чист; HF-копия 18/18. ⚠️ **Старые ключи прода** `chance_base_pct`/
  `chance_max_pct` больше не читаются → шанс сбросится к дефолту 30% (это и
  желаемо: старые значения вводили в заблуждение). **Sync ✅** (6 файлов,
  diff -q пуст). **PUSH ✅** (2026-06-14) GitHub `eaacb5b..01bfd04` (Pages),
  HF Space `71c52f1..03c5d54`.
- [ ] **F1. Переверстать `ProfileScreen`.** Крупная аватарка по центру → сводка
  (сколько раз был лохом / чуханом) → кнопка «Топы» → История. В истории
  показывать не только участник+дата, но и **причину** (у лоха причина уже есть
  в `/api/loser/history`/`fetchLoserReason`; свериться, что отдаём и рендерим).
- [ ] **F2. Приветственный попап: описание + тогл в одну строку.** Сейчас блок
  занимает ~четверть экрана — ужать до single-line (label слева, switch справа).
- [ ] **F3. «Формат участника» — настройка в одну строку** (тот же single-line
  паттерн, что F2; вероятно тот же экран настроек приветствия/профиля).
- [ ] **F4.** Тесты (если затронут бэк) + sync + push HF/GitHub.

### G. Антиспам планировщика (прод-фидбек п.14, п.21) — ПРИОРИТЕТ
Контекст (пользователь, 11.06): хрупкий бесплатный сервер + узкий канал
(РКН-throttling). Половина job'ов — поллинг «а не пора ли», хотя событие
редкое или НИКОГДА не наступало в проде. Главное требование: **свести весь
периодический трафик (исходящий к TG + SELECT в Neon) к минимуму; для
оставшихся частых опросов согласовывать частоту с пользователем перед кодом.**
⚠️ Любую смену интервала/частоты НЕ коммитить без явного «ок» от пользователя.

Инвентаризация на 11.06 (по `app/bot/scheduler.py`, проверено):
- `proxy_health` — ✅ ГОТОВО (11.06, не запушено): job регистрируется ТОЛЬКО в
  режиме `ALWAYS_ON`. Было `IntervalTrigger(3600s)` безусловно = 24 холостых
  getMe/сутки; алёрт про мёртвые прокси и так срабатывает лишь в ALWAYS_ON, а
  AUTO_FALLBACK управляется per-request в dispatcher (тик его НЕ питает —
  проверено). В проде режим AUTO_FALLBACK/ALWAYS_OFF → **0 запросов**. Смена
  режима из админки (`PUT /admin/proxy/mode`) дёргает `reschedule_proxy_health`.
  `scheduler.py` (reschedule_proxy_health + перевзвод на старте), `routes_admin.py`
  (PUT). Без юнит-теста (нет async-scheduler+БД стенда; логика тривиальна),
  сьют 376.
- `loser_outbox_retry` — `IntervalTrigger(5мин)`. 288 SELECT/сутки в Neon; по
  словам пользователя ни разу не сработал по назначению. Он вручную сдвинул.
- `bot_pause_auto_restore` — `IntervalTrigger(5мин)`. 288 SELECT/сутки; крутится
  даже когда паузы нет (а это 99% времени).
- `space_restart_tick` — ✅ ГОТОВО (11.06, не запушено): переведён на событийную
  модель. Был `IntervalTrigger(5мин)` = 288 холостых SELECT/сутки → теперь один
  `DateTrigger` на точное время (`compute_next_fire`, учёт 30-мин анти-лупа);
  при `mode=off` job не регистрируется = **0 запросов в простое**. Перевзвод на
  старте + после выстрела (self-rearm) + из admin PUT. `space_restart.py`
  (compute_next_fire), `scheduler.py` (reschedule_space_restart), `routes_admin.py`
  (PUT). +6 тестов, сьют 376.
- `chukhan_retry` — `IntervalTrigger(30мин)`. 48 SELECT/сутки.
- `dead_chat_hourly` — `IntervalTrigger(1ч)`. 24 SELECT/сутки; пользователь:
  «возможно осмысленно, но хочу гораздо реже».
- `birthdays_daily` — `CronTrigger(9:07/сутки)`. 1/сутки. Пользователь спросил
  «зачем каждые 9 часов» — на деле раз в СУТКИ (он прочёл «9» как интервал);
  всё равно ставит вопрос «достаточно и пары раз в неделю».
- `autoloser` — по расписанию; пользователь: «не пролетает в чат когда ловим
  блок за спам» (связано с блоком F-баги/Q-NET, не чисто частота).
- `meeting_reminders_tick` — `IntervalTrigger(tick_minutes)`, кламп 1..120 мин
  (`admin_config.get_reminders_tick_minutes`). П.14: «не могу сделать реже
  120 мин, а по-хорошему раз в ~6 часов».

Принцип решения (обсудить, НЕ кодить без согласования):
- Поллинг ради «не пора ли» с редким/никогда-событием → перевести на
  **событийное one-shot планирование** (DateTrigger на точное время) ИЛИ не
  регистрировать job совсем, пока в очереди пусто. Шаблон: автолох уже умеет
  `DateTrigger` + переустановку в `reload_dynamic_jobs`.
- `proxy_health` при «прокси никогда не работали» — кандидат на отключение
  по умолчанию (env-флаг/admin-toggle), либо резкое урежение.
- `bot_pause_auto_restore`/`space_restart_tick`/`loser_outbox_retry` —
  поднять интервалы и/или включать job только когда есть отложенная работа
  (pending-строка в outbox / активная пауза / включённое расписание рестарта).
- Поднять верхний кламп `reminders.tick_minutes` со 120 до ~360+ (п.14).
- `birthdays_daily` оставить раз в сутки (1 запрос — не спам), но обсудить.

Подход (согласовано 11.06): идём по ОДНОМУ job'у за раз — обсуждаем целевое
поведение, выбираем вариант, кодим, фиксируем, push пачкой в конце блока.
- [x] **G.space_restart** (11.06) — событийная модель, см. строку выше. PUSH ⏳
- [x] **G.proxy_health** (11.06) — job только в ALWAYS_ON, см. строку выше. PUSH ⏳
- [ ] **G.loser_outbox_retry** — обсудить (5мин, 288/сутки, ни разу не сработал).
- [ ] **G.bot_pause_auto_restore** — обсудить (5мин, крутится без активной паузы).
- [ ] **G.chukhan_retry** — обсудить (30мин).
- [ ] **G.dead_chat** — обсудить (1ч; пользователь хочет гораздо реже).
- [ ] **G.birthdays** — обсудить (раз в сутки 9:07; «пары раз в неделю хватит»).
- [ ] **G.meeting_reminders_tick** — обсудить (кламп 1..120, п.14 хочет ~6ч).
- [ ] **G.PUSH** — пачкой: sync `backend/` → `meetup-planner-backend/`, diff,
  коммит, push HF + GitHub (если фронт затронут).

### Q-NET. Сетевая отказоустойчивость (Q8/Q9/Q10) — архитектура
Контекст: РКН душит прокси, всё идёт через direct; джобы зависают, помогает
свап proxy↔direct или смена VPN на телефоне; рестарт Space «расклинивает».
НЕ трогать первый (зелёный) прокси (Q8). Кнопка «найти живые прокси» никогда
не работала — низкий приоритет (Q8).

> **Статус (2026-06-12):** Q-NET.b реализован и задеплоен (см. архив GHG7).
> Остаётся a; c — рекомендация отклонить. **d (переключатель Webhook↔Polling)
> свёрнут** как порождение инцидента 10–12.06: вебхуки лежат из-за внешней
> РКН-блокировки, переключатель транспорта её не лечит. Кодить a только после
> согласования с пользователем.

#### Q-NET-INV: план (2026-06-06, по коду dispatcher.py/proxies.py)

**Диагноз «зависает, рестарт расклинивает»:** aiohttp `TCPConnector` держит
keep-alive-пул; при throttling РКН соединение полудохнет, но остаётся в пуле и
переиспользуется → каждый следующий запрос виснет на нём до 30с. Рестарт Space
= новый коннектор = «расклинило». Свап proxy↔direct помогает по той же причине:
`_swap_session` пересоздаёт `ClientSession` (dispatcher.py:101). ⇒ ядро решения —
**уметь пересоздавать сессию без рестарта и не дать дохлым keep-alive жить**
(базу под это уже заложил Q-NET.b — см. архив).

- **(a) Circuit Breaker** — модуль `app/services/net_breaker.py`: счётчик
  последовательных сетевых фейлов в `make_request` (там уже ловятся
  ClientConnectorError/ClientOSError/ServerDisconnected/Timeout,
  dispatcher.py:167). N фейлов подряд (дефолт 3, admin_config) → «трип»:
  принудительный `_swap_session(текущий transport)` — пересборка ClientSession
  с тем же путём (направление НЕ меняем — Q8), + лог + admin-алёрт
  (rate-limit как у proxy down). Half-open: после успеха счётчик в 0.
  Состояние — module-level (как `_state` в proxies). Без новых зависимостей.
- **(c) curl_cffi** — РЕКОМЕНДАЦИЯ: отклонить. aiogram завязан на
  aiohttp-сессию; подмена транспорта = форк-обёртка над всем Bot API клиентом,
  поломает smart-proxy и (a)/(b). TLS-fingerprint для api.telegram.org —
  гипотеза, факты логов на неё не указывают.
- **(d) Webhook ↔ Long Polling** — ❌ СВЁРНУТ (12.06). Возник в рамках инцидента
  10–12.06 как обход блокировки вебхуков; первопричина внешняя (РКН), смена
  транспорта её не решает. Если дойдёт до миграции/переезда — поднять заново
  отдельным блоком, не тащить в Q-NET.

Рекомендуемый порядок: **только a** (b сделан; c — отклонить; d — свёрнут).

- [ ] **Q-NET.a (10а).** Circuit Breaker / авто-fallback (план (a) выше).
- [ ] **Q-NET.c (10б).** TLS fingerprint / curl_cffi — INV-оценка, реализация
  под вопросом (рекомендация — отклонить, см. (c)).
- [ ] **Q-NET.e.** Тесты + sync + DEPLOY_NOTES для a (для .b уже сделано).

### P6.1.b. Сидинг 6 персоналий (ручной шаг пользователя)
- [ ] **P6.1.b.** Сидинг 6 персоналий (GHG7.txt стр. 154–159) — руками
  пользователя через админку («🎭 Персоналии», формат [шаблоны]/[слоты]).
  Тексты живут в Neon, не в git. Остальное по P6 закрыто (см. архив GHG7).

## Отложено явно (НЕ в этой итерации)
- **Бот сам постит мемы.** GHG7.txt стр. 203–204 — «Пока не реализуем».
- **Реальный цитатор** (P6.4 из GHG7) — требует индекса сообщений в Neon.

###
Добавлено 13.06.2026
1. Придумал гениальную тему, которую крайне просто реализовать. Команда /advice или же хештег #advice или #совет упомянутый в сообщении - бот дает рандомный совет аналогично магическому шару. Запили туда порядка 10-20 разнообразных вариантов в нашем стиле от дефолтных "Да, нет, точно, возможно, не факт" до приколюшечных но чтобы они попадали в смысл. Так добавим возможность включать эту функцию и редактировать фразы из админки. Рандом честный, без счетчиков использования.
Альтернативный триггер: юзер может задать любой вопрос "А не пора ли встретиться, что скажешь? @gunghoguns_bot" либо "@gunghoguns_bot а не лох ли ты часом?" если в сообщении в любом месте упоминается (не важно до или после вопросительного знака) @gunghoguns_bot и оно заканчивается на вопросительный знак, это однозначный триггер на то что нужно ответить на сообщение любым из вариантов как при /advice . Важно чтобы бот понимал разницу где просто цитирование, если вопросительного знака в конце нет, то сценарий такой как раньше был при отметке бота\реплае.
2. На отдаленное будущее можно замутить такую схему что червь-пидор это не просто плашка-статус, оно еще и символизирует что ты как бы становишься на этот период хозяином бота и он твой прислужник. А выражаться это будет в следующем:
а) Если юзеру который в данный момент является червем, выпал неудачный статус типа как лох или чухан, то все анонсы работают как и раньше, но при этом бот выражает свое соболезнование, в стиле: 
Лох дня - Серж, причина: блабла 
Но при этом бот добавляет подхалимский префикс в духе "С прискорбием вынужден сообщить" "Новость печальная, но тем не менее" "Мой Господин %username$, вынужден вас расстроить" либо суффикс после "Да, новость не из приятных" "Я тоже с этим глубоко не согласен, милорд" "Вопиющая несправедливость" "Ваша неудача и мое личное поражение, милорд %username%" и т.д. Нужен набор от 5 до 10 подхалимских префиксов и суффиксов с возможностью редактирования (можно смело делать отдельный блок настроек под червя с редактированием шанса и принудительным назначением роли юзеру с анонсом в чат и без). Нужно чтобы фразы чередовались более-менее без частых повторов, можно сюда подбить механику счетчика использования.
б) Режим поддакивания. На любое сообщение господина (нужны варианты настройки, рандом например на каждые 3-5 сообщений, или каждое сообщение крутит шанс на триггер поддакивания, или на тайм интервал - поддакивание не чаще раза в 1 час или 5 часов - всё настраиваемо в блоке настройки червя). Само поддакивание проявляется так:
Юзер общается в чате, бот выбирает в зависимости от условий сообщение господина и цитируя его - комментирует в положительном ключе "Удваиваю!" "Истина." "Каждая фраза - как молитва!" "Господин %username% не может ошибаться." "Документально подтверждено." и тд. тоже с запасом в 5-10 фраз редактируемых. Нужно чтобы фразы чередовались более-менее без частых повторов, можно сюда подбить механику счетчика использования. Режим поддакивания отдельно отключаем в админке, так и самим пользователем-червем через команду /отвали (или типа того). Периодически, примерно на каждые 5-10 поддакиваний бот может напоминать "Если утомляю вас навязчивостью, милорд %username% - можете использовать команду /Отвали"
в) Возможность наказать кого либо в словесной перепалке, натравив на него бота. Только участник со статусом червя может использовать команду /punish @user либо же #punish @user либо #наказать @user (не уверен что все это можно реализовать прямо в таком виде, но суть ты понял) и бот выкатит анонс в стиле текстовых рпг о том что произошло, например я выбрал /Punish @серж и бот выкатывает "Влетаю с двух ног в грудину @серж" или "Выливаю ушат говна с высокой башни на голову @серж" или "Плеснул банку застоявшейся мочи прямиком на рабочий стол говноеду @серж" - тоже запас порядка 20 фраз с счетчиками и редактором в админке.
г) При анонсе того что кто-то стал червем, помимо упоминания о том что это невероятная удача и супер редкая фишка с шансом (указать текущий шанс, в том числе если он был изменен в настройках), нужно так же максимально кратко но доступно описать, что это за собой повлечет. Что-то в стиле: "С данной минуты, %username% - мой великий господин. Я буду стараться во всем ему угождать и поддерживать и подчиняюсь прежде всего его воле. А если недруги застанут вас в расплох, то расквитаюсь с ними я по команде /punish @user"
