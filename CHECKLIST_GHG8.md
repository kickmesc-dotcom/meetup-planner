# Meetup Planner — чеклист GHG8

Преемник `CHECKLIST_GHG7.md` (архив релиза P0–P10 + **все закрытые блоки GHG8**,
см. там секцию «ЗАКРЫТО GHG8»). Здесь — **только то, что предстоит сделать**.
Детали закрытых задач — `git log --grep "GHG8"`.

Single source of truth: `C:\Users\fa1nt\meetup-planner-main` (монорепо
`backend/` + `frontend/`). После правок `backend/` —
`cp backend/* → C:\Users\fa1nt\meetup-planner-backend\` (отдельный git remote).
Push в HF/GitHub делает ассистент (git PAT) ИЛИ пользователь —
фиксируется в каждом sync-подэтапе.
⚠️ **ДВОЙНОЙ ДЕПЛОЙ БЭКА (приказ 30.06):** пока активны оба хоста, push бэка
ведём ПАРАЛЛЕЛЬНО — `git push origin main` (HF) + `git push amvera main:master`
(Amvera, ветка master!). Действует до внедрения рубильников-переключателей.
⚠️ Push в GitHub через credential.helper=manager ВИСНЕТ (молча, >5 мин) —
пушить с PAT в URL:
`git push https://x-access-token:<PAT>@github.com/kickmesc-dotcom/meetup-planner.git main`.

## Легенда статусов
- `[ ]` — не начато · `[~]` — в работе (обрыв/ожидание) · `[x]` — закрыто

## Правила работы с чеклистом
0. **ДИСЦИПЛИНА МЕТОК (приказ пользователя 30.06).** Метку `[~]` ставим
   **ПЕРЕД** тем как взять подзадачу в работу, метку `[x]` — **СРАЗУ ПОСЛЕ**
   её завершения. Цель: при обрыве сессии достоверно видно, на чём
   остановились — никаких «вдруг найденных» полуготовых кусков кода без следа
   в чеклисте. Если подзадача написана, но не закоммичена/не задеплоена — она
   `[~]` с пометкой «код готов, не запушено», НЕ `[x]`.
1. Этапы разбиты на подэтапы `PX.Y` / `PX.Y.a`.
2. После закрытия подэтапа — `[x]` + однострочная пометка
   `(YYYY-MM-DD) <что сделано> — <ключевой файл/коммит>`.
3. Sync `backend/` → `meetup-planner-backend/` — отдельный подэтап с галкой.
4. Обрыв → `[~]` с пометкой, на каком файле/шаге остановились.
5. Investigate-пункты (`INV-N`) не «фиксят», а отвечают на вопрос и порождают
   follow-up подэтапы; закрываются выводом + ссылкой на них.
6. **Закрытый блок целиком → переносим однострочником в `CHECKLIST_GHG7.md`
   («ЗАКРЫТО GHG8») и удаляем отсюда.** Рабочий файл держим компактным —
   только открытое, чтобы не таскать готовый код в контекст каждой сессии.

Состояние прода (2026-06-12): HF backend на `9d5c702` (HOTFIX BOT_API_SERVER).
⚠️ Открыт ручной шаг P6.1.b — сидинг 6 персоналий через админку «🎭 Персоналии»
(тексты в Neon, не в git). ⚠️ env `HF_TOKEN` (write-токен HF для рестарта Space,
P14) — добавить руками в секреты Space; без него кнопка рестарта дизейблится,
остальное работает. Q-NET.b добавил опц. env-тюнеры пула/таймаутов
(`BOT_KEEPALIVE_TIMEOUT`/`BOT_TOTAL_TIMEOUT`/`BOT_SOCK_CONNECT_TIMEOUT`/
`BOT_FORCE_CLOSE`) — все с дефолтами, руками выставлять НЕ обязательно. Прочее —
в `admin_config`.

⚠️ **Egress HF→Telegram (инцидент 11–12.06).** Прямой исходящий из HF Space в
`api.telegram.org` не проходит (timeout) — вход (webhook) при этом жив. Бот
«слепой»: апдейты получает, ответить не может. Лечится env `BOT_API_SERVER` =
URL cloudflare-воркера-реверс-прокси (worker переписывает host→api.telegram.org).
Прод сейчас: `BOT_API_SERVER=https://telegram-proxy.kickmesc.workers.dev`
(выставлен в variables Space). Пусто = direct (мёртвый канал). Запасной воркер —
`telegram-proxy2.esw1337.workers.dev`. Оба проверены живыми (getMe/sendMessage
ok). См. [[incident_meetup_planner_sock_connect_blind]] (это ДРУГОЙ, более ранний
слой того же «бот молчит» — sock_connect=8, уже починен).

---

## 🔓 РАЗМОРОЗКА + НОВЫЙ ПРИОРИТЕТ (решение пользователя 30.06.2026)
Заморозка 15.06 **ОТМЕНЕНА**: переезд на Amvera снял egress-блок РКН
(HF→Telegram), бот больше не «слепой» — косвенные правки коннекта/постинга
больше не табу. Якорь отката всё ещё есть: git-тег `stable-2026-06-15`.

**Стратегия хостинга (важно для всех правок планировщика):** постоянный
платный канал НЕ оправдан — за неделю простоя бота никто из шестёрки не
заметил, интерес упал. ⇒ **дефолт = старый бесплатный воркфлоу (HF), платный
Amvera подрубаем ВЫБОРОЧНО** (периоды сбоев / исчерпание фри-лимитов). Поэтому
урезание необязательных опросов (**блок G**) остаётся в полной силе — каждый
лишний SELECT/getMe буквально жрёт баланс. Любую смену интервала/частоты
по-прежнему согласовываем перед коммитом (правило блока G).

**Приоритеты (порядок работы):**
1. **Пустить в прод готовый код** — T3.6.6 (редактор фраз червя) уже написан,
   лежит незакоммиченным → закрыть рассинхрон `/punish`↔редактор.
2. **Рубильники + урезание опросов (блок G) + запрошенные фиксы.**
3. **Новые фичи (backlog)** — второстепенно, внедряем неспешно в окна между
   приоритетными проектами (предложка музыки, мьюзик-гейм, анонс топ-1 и т.д.).

## 📋 ПЛАН Tier 2 → Tier 3 → Tier 1 (история 15.06, ниже)
Принцип «дёшево+быстро вперёд». Отмечаться в чеклисте после КАЖДОГО подэтапа.

**Tier 2 (средне, без коннекта):**
- [x] **T2.1 (п.5/10).** (2026-06-15) Автоподбор схлопывал пересекающиеся
  пресеты. Диагноз: `auto_pick.py:150` жадный де-оверлап оставлял ОДИН
  непересекающийся слот из `17-21/18-22/19-23/20-00` → `len(slots)<2` →
  `not_enough_slots` (routes_polls.py:122), опрос не стартовал. Фикс: в
  preset-режиме де-оверлап отключён (`return slots[:top_n]`) — пресеты это
  курируемые варианты для голосования, нахлёст допустим; fallback скользящего
  окна де-оверлап сохраняет. Бэк-only (фронт-гейт `>=2` срабатывает сам). +3
  теста (`test_auto_pick_overlap.py`), сьют **392 passed**. **Sync ✅**
  (auto_pick.py + тест, diff -q пуст). **PUSH ✅** (2026-06-16, в T2.PUSH).
- [x] **T2.2 (п.15).** (2026-06-16) Валидация кастомных emoji-реакций. Статический
  набор `TELEGRAM_ALLOWED_REACTIONS` (≈70 эмодзи из Bot API `available_reactions`,
  БЕЗ live-API → коннект не трогаем), нормализация по VS16 (U+FE0F) — `❤`/`❤️`
  оба валидны. `set_emoji_whitelist` отсекает неподдерживаемое и ВОЗВРАЩАЕТ
  отброшенные; новый `MediaEmojiWhitelistOut{phrases,rejected}` (routes_admin.py)
  прокидывает их на фронт → `MediaReactionsScreen` алёртит «TG не принимает как
  реакцию: … — не сохранены» (раньше код глотал молча, вопреки докстрингу). +7
  тестов (`test_media_reactions.py`: is_allowed_reaction/filter_allowed_reactions),
  сьют **399 passed**, tsc чист. **Sync ✅** (media_reactions.py + routes_admin.py
  + тест, diff -q пуст). **PUSH ✅** (2026-06-16, в T2.PUSH).
- [x] **T2.3 (п.13).** (2026-06-16) Переподписаны контролы «🤖 Реакции бота».
  (а) @-упоминание — независимый тогл. (б)/(в) поведение reply раньше было двумя
  путаными независимыми свитчами (`reply_all`/`reply_except_phrases`), хотя в бэке
  это ВЗАИМОИСКЛЮЧАЮЩИЙ выбор (`reply_all` перекрывает `except`, bot_reactions.py:
  157-160). Теперь один сегментированный выбор: Выкл / На все reply (б) / Кроме
  цитат (в, дефолт). Маппинг радио↔3 флага без потерь (`toReplyMode`/
  `replyModeFlags`, чтение даёт приоритет `all` — как бэк). Чистый фронт
  (`BotReactionsScreen.tsx`): контракт `BotReactionsSettings` (3 bool) и механизм
  отправки НЕ тронуты. ⚠️ снят: посты-реакции (`set_message_reaction`) живут в
  отдельном `media_reactions.py`, этот экран их не касается. tsc чист, бэк не
  затронут (sync не нужен). **PUSH ✅** (2026-06-16, в T2.PUSH).
- [x] **T2.4 (п.20).** (2026-06-16) «Прыгающее» меню админки. Диагноз: карточки
  корня `AdminScreen.tsx` СТАТИЧНЫ (хардкод, не data-driven) → между собой не
  рефлоутятся. Единственный элемент сверха, меняющий высоту на обычном открытии —
  `BotPauseBar`: `isPending` (1 строка) → резолв в компактную плашку «в эфире»
  (2 строки) толкал ВСЁ меню вниз, в т.ч. критичные Прокси / Рестарт Space (ровно
  кейс п.20 «тыкнул не туда»). Лёгкий фикс (не реворк дерева, как и просил
  пользователь): pending/error/idle делят `min-h-[3.75rem]` = высота резолв-плашки
  → карточки ниже не сдвигаются. Active-пауза (sticky-алёрт) не тронута. Чистый
  фронт (`BotPauseBar.tsx`), tsc чист, бэк не затронут. **PUSH ✅** (2026-06-16, в T2.PUSH).
- [x] **T2.PUSH.** (2026-06-16) Tier 2 запушен пачкой. Бэк (HF Space, 5 файлов:
  routes_admin/auto_pick/media_reactions + 2 теста, сьют 393) — `83bad5e..1ab618d`.
  Монорепо (GitHub Pages, 10 файлов = бэк-T2 + фронт T2.2/T2.3/T2.4 + чеклист) —
  `b29eb73..91eaab1`. Выборочный `git add` — замороженный WIP блока G
  (`scheduler.py`/`space_restart.py`/`test_space_restart.py`), T1.1-WIP
  (`AdminScreen.tsx`) и удаление `CHECKLIST_GHG7.md` (T1.6) оставлены
  незакоммиченными в рабочем дереве.

**Tier 3 (крупные фичи — обсуждать дизайн перед кодом):**
- [x] **T3.1 (п.11.1).** (2026-06-17) Снапшот/экспорт базы причин-реакций.
  Новый `services/phrase_snapshot.py`: `build_snapshot` собирает ВСЕ
  редактируемые пулы (loser/chukhan/advice + media single/collection/emoji) +
  use_counts (лох/чухан) + персонажей (типажи из `participant_personas`,
  ключуются по **telegram_id** — переживают пересборку БД). `apply_snapshot`
  с `mode=replace|merge` (use_counts только в replace, иначе веса разъедутся);
  `validate_snapshot`/`merge_pool` — чистые. Бэк-API `GET /admin/phrases/snapshot`
  + `POST /admin/phrases/snapshot/import {snapshot, mode}` → summary. Фронт
  `PhrasesSnapshotScreen.tsx`: экспорт (превью-счётчики + textarea + 📋копировать +
  💾файл через Blob) и импорт (вставить/загрузить файл → JSON-превью → выбор
  слияние/замена → подтверждение). Карточка «🗄 Снапшот фраз» в новой группе
  «Данные». Механика — clipboard (надёжно в TG) + файл (бонус для десктопа), как
  выбрал пользователь. ⚠️ полный дамп Neon НЕ делаем — только пулы фраз/типажи
  (как и просили: «слепок базы причин/реакций»). +12 тестов
  (`test_phrase_snapshot.py`), сьют **419 passed**, tsc чист. **Sync ✅** (3 файла
  бэка, diff -q пуст). **PUSH ✅** HF Space `a1f7ddb..9fcf248`, GitHub Pages
  `63b5786..4b7a4bc`.
- [ ] **T3.2 (п.11).** Единый «хаб фраз» всех режимов (лох/чухан/реакты) +
  swap фразы между разделами.
- [x] **T3.3 (п.18).** (2026-06-17) Блок алертов «лох/чухан не запостился».
  Новый `services/posting_alerts.py` — **read-only** сводка терминально
  застрявших: лох = `loser_outbox.status='expired'` (исчерпал 12×5мин ретраев),
  чухан = запись текущей недели `posted_at IS NULL` старше форы (1ч, чтобы не
  шуметь пока штатный 30-мин ретрай работает). Чистые `is_chukhan_overdue`/
  `summarize`. ⚠️ **Заморозка соблюдена**: по решению пользователя outbox лоха
  НЕ трогаем (он работает идеально, никогда не виснет) — только читаем; кнопки
  перепрогона лоха НЕТ. Перепрогон ТОЛЬКО для чухана через существующую
  `retry_undelivered_chukhan` (нет outbox, штатный путь, без «алерта в чат что
  вручную»). API `GET /admin/posting-alerts` (read-only) + `POST .../chukhan-retry`.
  Фронт `PostingAlertsScreen.tsx`: при пропуске чухана — алёрт+кнопка «дослать»,
  при expired-лохе — информ-список (без кнопки), пусто → «✓ всё доставлено».
  Карточка «🚨 Алёрты постинга» **рядом с прокси** (как просил пользователь) с
  бейджем-счётчиком (обычно 0). +10 тестов (`test_posting_alerts.py`), сьют
  **429 passed**, tsc чист. **Sync ✅** (3 файла бэка, diff -q пуст). **PUSH ✅**
  HF Space `9fcf248..695700e`, GitHub Pages `42f332d..3764b5a`.
- [x] **T3.4 (13.06 #1).** (2026-06-17) `/advice` / `#совет` / `#advice` + триггер
  «@bot …?» — рандомный совет (магический шар). Новый `services/advice.py`
  (чистые `pick_advice`/`text_has_advice_hashtag`/`ends_with_question`, честный
  рандом БЕЗ счётчиков — как просил пользователь) + пул в `admin_config`
  (`advice.list`/`advice.enabled`, дефолт ~20 фраз в стиле группы, подхват из кода
  пока нет кастома — как loser_reasons). Команда `/advice` (chat_commands.py,
  whitelist-гейт) + `reply_advice()` reusable. Альт-триггеры в `bot_reactions.py`
  `_maybe_react` ДО обычной реакции: хештег #совет/#advice (независим от тоглов)
  и @-mention + текст на «?» (без «?» — старый сценарий рандом-фразы). Оба гейтятся
  `advice.enabled`. Каталог команд `/advice` (видна в /help и menu). Бэк-CRUD
  `/admin/advice` (GET + PUT phrases/enabled). Фронт: `AdviceScreen.tsx` (Toggle +
  reusable `ReasonsEditor` без счётчиков), карточка «🔮 Магический шар» в группе
  «Реакции». ⚠️ `bot_reactions.py` затронут, но ТОЛЬКО реактивный reply-путь —
  планировщик/постинг/коннект НЕ тронуты (frozen-зона цела). +8 тестов
  (`test_advice.py`), сьют **407 passed**, tsc чист. **Sync ✅** (7 файлов бэка,
  diff -q пуст). **PUSH ✅** HF Space `1ab618d..a1f7ddb`, GitHub Pages
  `7ca00a6..c502817` (вместе с T1.1).
- [ ] **T3.5 (п.8).** Развёрнутая инструкция к редактору персоналий +
  продуктовое решение «рандом/LLM vs автовыплёвыватель».
- [~] **T3.6 (13.06 #2).** Режим «червь-господин» (подхалимаж/поддакивание/
  `/punish`/расширенный анонс). Дизайн согласован, идём подэтапами T3.6.1–8;
  рисковое поддакивание (б) — последним отдельным подэтапом T3.6.8 после пуша
  безопасной части (решение пользователя). Ручное назначение господина НЕ делаем
  (экран read-only). ⚠️ **PUSH #1 ушёл раньше плана** (после T3.6.5, не T3.6.7) —
  решение пользователя 23.06 «запушить всё готовое». Бэк-логика червя gated
  `enabled=False`, в проде бездействует. **Отсюда рассинхрон: `/punish` светится
  в `/help`/menu (она в commands_catalog), но это no-op (gated), а редактора
  фраз ещё нет в проде.** Осталось: **T3.6.6** (админка червя — КОД ГОТОВ, не
  запушен), **T3.6.7** (?), затем **T3.6.8** (поддакивание).
  - [x] **T3.6.1.** (2026-06-22) Фундамент: пулы фраз + конфиг, без поведения.
    Новый `services/worm_master.py` — чистое ядро (без БД/aiogram): дефолтные
    пулы (prefixes/suffixes/agrees/nag/punish ~20/announce-lines) + `choose`
    (сырой выбор, взвеш. опц.), `render` (безопасная подстановка
    `{username}`/`{target}`/`{chance}` через `.replace`, чужие `{...}` не роняют),
    `build_announce_extra`, `pick_nag`. ⚠️ `choose` намеренно отдаёт СЫРУЮ фразу
    (её хэш = ключ use_counts → инкремент по ней, отображение через `render`).
    В `admin_config.py`: тумблеры `worm_master.enabled` (default **False** —
    поэтапный выкат), `punish_enabled`/`yes_enabled`/`yes_pct`(15)/
    `yes_cooldown_min`(60) + get/set 6 пулов (хелперы `_get_pool`/`_set_pool`/
    `_dedup_clean`, cleanup use_counts). `phrase_weights.py`: 4 новых
    use_counts-ключа. +16 тестов (`test_worm_master.py`), весь сьют **457 passed**.
    **Sync ✅** (worm_master/admin_config/phrase_weights + тест, diff -q пуст).
    **PUSH ✅** (PUSH #1, HF `767586c..5700d5f`).
  - [x] **T3.6.2.** (2026-06-22) Расширенный анонс червя (часть г). Новое поле
    `WormEvent.announce_extra` (`loser.py`): в `roll_loser` при `is_worm` И
    включённом `worm_master.enabled` собираем блок «что даёт звание господина»
    через `build_announce_extra` (пул `worm_master.announce_lines`, подстановка
    `{username}`=имя лоха, `{chance}`=шанс в % через `:g`). Заполняем ОДИН раз до
    ветвления назначения → оба `on_announce`-пути (продление того же червя +
    новое назначение) получают текст. `compose_loser_message` рендерит его в
    worm-ветке ДО роллера/счётчика (форматтер чистый, БД не дёргает). Режим
    выключен → `announce_extra=None` → анонс как раньше. +2 теста (`test_worm.py`),
    сьют **459 passed**. **Sync ✅** (loser.py + test_worm, diff -q пуст).
    **PUSH ✅** (PUSH #1, HF `767586c..5700d5f`).
  - [x] **T3.6.3.** (2026-06-22) Подхалимский префикс/суффикс при лохе-господине
    (часть а, лох). Новые `MasterSycophancy{prefix,suffix}` + `RollExtras.worm_master`
    (`loser.py`). Хелпер `resolve_master_sycophancy(session, user)`: если режим
    `worm_master.enabled` И `user` == текущий `get_current_worm` → взвеш. выбор
    `choose`(pool, use_counts) из prefix/suffix-пулов, инкремент по СЫРОЙ фразе,
    `render` подставляет имя. Зовётся в `roll_loser` ТОЛЬКО при `not is_worm` (на
    становлении червём — особый анонс, не подхалимаж). `compose_loser_message`
    обычная ветка: префикс `<i>` первой строкой, суффикс последней. Хелпер
    переиспользуем чуханом (T3.6.4). +5 тестов (compose + fake-session гейт
    выкл/не-червь; реальный путь — руками, паттерн проекта), сьют **464 passed**.
    **Sync ✅** (loser.py + test_worm, diff -q пуст). **PUSH ✅** (PUSH #1, HF `767586c..5700d5f`).
  - [x] **T3.6.4.** (2026-06-22) Подхалимаж при чухане-господине (часть а, чухан).
    `_format_announcement` принял опц. `master_prefix`/`master_suffix` (префикс
    `<i>` сверху, суффикс снизу; тело перешло со склейки `\n`-строки на
    list-join — формат байт-в-байт сохранён, старые тесты зелёные). В
    `announce_chukhan` резолвим через переиспользуемый
    `resolve_master_sycophancy(session, user)` ТОЛЬКО в else-ветке (свежий пост,
    рядом с выбором причины) — на ретрае `row.reason_text` задан → не резолвим,
    use_counts не дублируются (подхалимаж в колонке не храним, на ретрае его нет
    — приемлемо). ⚠️ Постинг/дробь/таймаут НЕ тронуты (только форматтер +
    резолв до отправки, прецедент T1.2). +3 теста (`test_chukhan_reason.py`),
    сьют **467 passed**. **Sync ✅** (chukhan.py + test, diff -q пуст).
    **PUSH ✅** (PUSH #1, HF `767586c..5700d5f`).
  - [x] **T3.6.5.** (2026-06-22) Команда `/punish` (часть в). Хендлер
    `on_punish`/`_handle_punish` (`chat_commands.py`): гейт ЖЁСТЧЕ whitelist —
    вызвать может ТОЛЬКО текущий `get_current_worm` + тоглы
    `worm_master.enabled`/`punish_enabled`; не-господину тихий no-op. Цель:
    сначала entities (`mention`/`text_mention`), фолбэк — текстовый
    `extract_punish_target`. `choose`(pool, use_counts) + инкремент по сырой
    фразе + `render({target})`. Альт-триггеры `#punish`/`#наказать` — в
    `bot_reactions._maybe_react` ДО обычных реакций (реактивный путь, прецедент
    T3.4; весь гейт внутри `_handle_punish`). `/punish` в `commands_catalog`.
    Пара чистых хелперов в `worm_master.py` (`text_has_punish_hashtag`,
    `extract_punish_target`). +9 тестов, сьют **473 passed**. **Sync ✅**
    (worm_master/chat_commands/bot_reactions/commands_catalog + тест, diff -q
    пуст). **PUSH ✅** (PUSH #1, HF `767586c..5700d5f`).
  - [~] **T3.6.6.** Админка червя: тогл/шанс/CRUD 6 пулов + экран
    `WormMasterScreen.tsx`. ⚠️ **КОД ГОТОВ В РАБОЧЕМ ДЕРЕВЕ, НЕ ЗАКОММИЧЕН/НЕ
    ЗАПУШЕН** (обнаружено 30.06). Файлы: `WormMasterScreen.tsx` (untracked),
    `routes_admin.py` (+148, worm-CRUD: settings + 6 пулов), `admin.ts` (+45),
    `AdminScreen.tsx` (+10, карточка «червь-господин»). Сплит от блока G —
    чистый по файлам (G живёт в `scheduler.py`/`space_restart.py`). Осталось:
    выборочный `git add` worm-файлов → тесты → sync backend → push HF+Pages.
    Приоритет №1 (новая стратегия 30.06).
- [ ] **T3.PUSH.**

**Tier 1 (мелкая полировка):**
- [x] **T1.1 (п.7).** (2026-06-17) «Расписание задач» → «Настройки задач»
  (карточка в `AdminScreen.tsx`). Закоммичен вместе с T3.4 (тот же файл —
  карточка «🔮 Магический шар» рядом), отдельный коммит не имел смысла. **PUSH ✅**
  (в T3.4-пуше GitHub).
- [x] **T1.2 (п.12).** (2026-06-18) Префикс «Причина:» в анонсе чухана + причина
  в сводной истории. Бэк: `_format_announcement` теперь выводит фразу под
  `<i>Причина: …</i>` (формат выровнен с лохом, `loser.py:376`). Причина РАНЬШЕ
  выбиралась на лету и нигде не хранилась → новая колонка
  `weekly_chukhan.reason_text` (миграция `0016_chukhan_reason`, nullable; старые
  недели без причины). `announce_chukhan` резолвит фразу ДО постинга (включая
  дефолтную ветку, раньше `random.choice` сидел внутри форматтера и терялся),
  пишет `row.reason_text` до отправки → на failure-ретрае та же фраза
  переиспользуется (без переролла и двойного инкремента use_count — закрывает и
  остаток «повтор причины» из 15.06 п.1). API: `reason_text` в `ChukhanWeekOut`
  (admin) и `ChukhanHistoryRow` (public/профиль). Фронт: типы
  `ChukhanWeek`/`ChukhanHistoryEntry` + рендер причины в чухан-вкладке
  `HistoryScreen.tsx` (по образцу лоха, `«…»` курсивом). +4 теста
  (`test_chukhan_reason.py`), сьют **433 passed**, tsc чист. **Sync ✅** (6 файлов
  бэка, diff -q пуст). **PUSH ✅** HF Space `695700e..767586c`, GitHub Pages
  `12e5ff0..320ce2d`.
- [x] **T1.3 (F2/F3).** (2026-06-18) Приветственный попап + «формат участника» в
  одну строку. Блок `GreetingSettings` (`ProfileScreen.tsx`) занимал ~четверть
  экрана (заголовок+описание, строка тогла, подпись+полноширинный сегмент из 3
  кнопок). Ужат в две single-line строки через `divide-y`: (F2) «👋 Приветствие»
  + подпись слева, `Switch` справа; (F3) «Формат» слева, компактный сегмент
  Аватар/Имя/Оба справа (`min-h-8 px-2.5`, `aria-label` с полными названиями).
  Контракт `UiPrefs` (`hide_greeting`/`welcome_format`) и мутации НЕ тронуты.
  Чистый фронт, tsc чист, бэк не затронут (sync не нужен). **PUSH ✅** GitHub
  Pages `945a24d..2c7b077` (фронт-only, HF не затронут).
- [x] **T1.4 (п.9).** (2026-06-21) Баг: «Типажи» показывает пусто, после возврата
  выбрана «нарезка». Диагноз: чип версии генератора (нарезка/типажи) был полем
  bulk-формы `GeneratorForm` — тап менял ТОЛЬКО локальный стейт, персист лишь
  вместе с «💾 Сохранить». Пользователь тапал «Типажи» → уходил в «Персоналии»
  смотреть тексты → возврат → refetch `rp-generator` + `useEffect([initial])`
  сбрасывал чип на сохранённую «нарезку» (выбор молча терялся). Фикс (фронт-only):
  версия вынесена в самостоятельный самосохраняющийся `GeneratorVersionBody`
  (своя query+мутация, как `ScheduleBody`/`GeneratorBody`) — **сохраняется сразу
  по тапу** (оптимистичный `setQueryData` + откат при ошибке, чтобы UI не прыгал).
  Bulk-сейв тюнинга теперь пробрасывает серверное значение версии (не затирает на
  `legacy`). Плюс предупреждение «ни одной персоналии не заведено → бот генерит
  нарезкой» когда выбраны Типажи, а пул пуст (`personas.filter(persona_text!=null)`)
  — объясняет то «пусто», из-за которого бэкенд молча фолбэчился
  (`random_phrases.py: personas_empty_fallback_legacy`). Контракт PUT не тронут →
  бэк-тесты/sync не нужны. Затронуты `RandomPhrasesGeneratorScreen.tsx` +
  `RandomPhrasesScreen.tsx`. tsc + vite build чисты. **PUSH ✅** GitHub Pages
  `34cdfdb..1f9fe3b` (фронт-only, HF не затронут).
- [x] **T1.5 (F1).** (2026-06-18) Переверстать `ProfileScreen`. Была компактная
  шапка (аватарка 48px слева, имя справа) → новый `ProfileHeader`: аватарка 96px
  по центру, под ней имя/@username, ниже сводка из двух ячеек «🤡 Был лохом N» /
  «💩 Был чуханом N». Счётчики для `me` берутся из тех же публичных эндпоинтов,
  что и Топы (`loser/stats.counts[me.id]` + `chukhan/leaderboard` find по
  `user_id`) — нового API не заводил. История (`HistorySection`): строки
  переписаны на аватар+имя+**причину** курсивом (`«…»`, `line-clamp-2`) — у лоха
  причина уже была в `/loser/history`, у чухана появилась после T1.2
  (`reason_text` в `/chukhan/history`). `GreetingSettings` (T1.3) сохранён как
  есть. Чистый фронт (`ProfileScreen.tsx`), tsc + vite build чисты, бэк не
  затронут (sync не нужен). **PUSH ✅** GitHub Pages ниже.
- [ ] **T1.6 (п.19).** Удалить `CHECKLIST_GHG7.md` (убедиться: всё закрыто/
  перенесено). Уже помечен на удаление в WIP.
- [ ] **T1.PUSH.**

## ОТКРЫТО

### F. Прод-фидбек после теста 10.06.2026
Источник — пользователь, дословно ниже. Низкий-средний приоритет, UI-полировка.

> 1. Если зайти в профиль, аватарка должна быть крупнее и по центру. Ниже
>    сводная инфа, типа как вы были лохом столько раз, чуханом столько раз.
>    Дальше едет кнопка топы. Дальше история, в истории главный смысл это видеть
>    не только участник+дата, но и причину.
> 2. Описание приветственного попапа и тогл нужно уместить на одной строчке,
>    это не такая важная фича чтобы занимать четверть экрана.
> 3. Формат участника тоже как настройка в одной строке.
Еще добавлено:
4. Аватарки перестали обновляться при ручном нажатии (как будто прод опять залип и висит, это поведение сохраняется уже пару недель)
5. Однажды я выставил ручные слоты времени и попытался нажать автоподбор, а он просто не дал мне ничего выбрать, поскольку было меньше трех вариантов, видимо из-за того что они оверлапились при виде
20:00 - 00:00, 19:00 - 23:00, 18:00 - 22:00, 17:00 - 21:00.
6. В очередной раз не сработал лох дня и просто исчез из очереди задач.
7. Постоянно путаю Запланированные задачи и Расписание задач. Кнопку расписание задач надо переименовать в Настройки задач
8. Для редактора персоналий мне нужна развернутая инструкция как его использовать и настраивать. Я изнаально планировал прописать характеры каждого участника и иметь возможность их дополнять, не только паттернами и примерами фраз, а в целом характерами, а за генерацию текста отвечало бы некое подобие LLM-ки. Здесь вообще не понятно как использовать, будет просто автовыплевыватель заготовленных фраз без какого либо рандома? Скука. 
9. При выборе версии Типажи я полез смотреть какие прописаны, там везде пусто, возвращаюсь обратно и выбрана нарезка. Надеюсь это преднамеренно.
10. Пресеты времени я уже указал выше наиболее адекватные, но при ручном изменении эти варианты просто исчезают из подставления в опросник без возможности начать опрос.
11. Немного неудобно что для каждого режима лох\чухан\реакты все подставлялки фраз лежат в отдельном меню. При этом это логично что они лежат в соотвествтвующих разделах, но хотелось бы по аналогии с интервалами иметь место где это все продублировано и собрано в кучу, можно было бы на лету глянуть сразу все фразы для всех режимов и даже свапнуть некоторые из них (кнопка рядом с фразой - удалить, отредактировать, переместить например из лоха в раздел чухан)
11.1 Еще я настолько много уже через интерфейс админки добавил клевых и смешных новых причин, удалив старые и несмешные, при этом не сохранив их в код\бд. (Это в принципе практически невозможно сделать когда у тебя по +100 позиций в каждом разделе). Будет обидно если они просто потеряются при следующем апдейте. Нужно что-то придумать чтобы я мог сохранить слепок текущей базы всех причин\реакций для всех режимов.
11.2. Там же, в реактах
12. В сводной истории в чухане отсутствует причина. Кстати, когда анонсируется чухан он просто пишет причину без приставки "Причина" иногда это конфузно фыглядит.
13. Реакции бота - здесь суперконфузно подписаны все пункты меню "ответ на реплай ответа" - что, я даже сам потерял связь и понимание для чего эти тогглеры. Нужно четкое разделение:
а) Бот всегда отвечает на упоминание через @ (вкл\выкл, по умолчанию вкл)
а здесь уже идет переключение между двумя разными функциями
б) Бот всегда отвечает на ВСЕ reply (если процитировать любое его сообщение из чата, включая анонсы, опросы, цитаты и т.д.)
в) бот отвечает на ВСЕ reply кроме ответов пользователей на сгенерированную цитату. Это сделано чтобы типы могли прокомментировать особо удачную шизо-цитату, но это не превращалось бы в переписку с пастой. (по умолчанию выбран этот вариант)
14. Тик напоминаний, авторебут, перепрооверка отправленности лоха\чухана (которая к слову ни разу не выполнила свою работу) - это все сейчас выставлено со слишком частыми интервалами и просто спамит пакетами наш беззащитный бесплатный сервак. Самое тупое что тик напоминаний я не могу даже сделать реже чем 120 мин. По хорошему такая вещь должна чекаться раз часов эдак в 6.
15. По системе реактов есть интерфейс добавления кастомных эмодзи реакций, что выглядело логично. Но потом я добавил туда много других вариантов и уже только потом вспомнил что не далеко каждое эмодзи можно выбирать как реакцию к посту. Здесь нужно либо изначально предлагать только те варианты которые 100% доступны как реакция, либо делать какую-то проверку на соответствие и отсекать любое подставленное юзером значение которое нельзя поставить в виде реакции.
16. Из-за того что статус бота залип, я весь день не могу принудительно прореактить на мем с кнопки (уже даже и не актуально), пишет что сообщение отправлено, но бот сегодня вообще ничего не присылал. А подборки он по прежнему криво и не всегда корректно распознает, в большинстве случаев просто не нашел. Скопировал лог в файл: incident10-06.txt
17. Лох дня накануне начал крутиться по два раза в день, хотя это не было задумано, но сегодня опять перестал срабатывать (из-за залипшего статуса), он пропал из планировщика задач, я зашел в блок настроек интервалов чтобы включить его заново и охренел от того как тупо и неочевидно все подписано. N - Каждые несколько часов, что вообще за бред. Мне нужны четкие и понятные режимы на выбор:
а) Есть предзаданный интервал привязанный к времени суток (сейчас 07 - 23), мы указываем сколько раз в течение этого интервала срабатывает автолох. Если 0 то 0, если 1, то 1, если 4 то 4. По клику принять программа сама распределяет время. Главное ограничить програмно чтобы не более 12 раз в интервале. 
б) Можно как с чуханом сделать просто ежедневный вариант, опять же в интервале, но например отключить по понедельникам. Настроить на один раз в день, с существующим интервалом 07 - 23 и выбрать как режим по умолчанию.
в) Выбрать конкретное время под каждый день недели. (Напр. в понедельник в 13:00 а в пятницу в 20:00)
18. Если в день в который назначен чухан или лох не было соответствующего поста в чатике, нужен некий блок алертов чтобы зашедшему в админку лишний раз это попало на глаза, можно там же где прокси закинуть где-то по сосетству. Там же сделать кнопку принудительного перепрогона (без алерта в чат что это было вручную) или возможностью в один клик запланировать в виде новой таски в течение ближайшего времени.
19. Я хочу полностью удалить файл checklist_ghg7, нужно убедиться что в нем либо закрыты все задачи либо перенесены в GHG8. Разработка должна стартовать с обращения к единому чеклисту без необходимости каждый раз заново лопатить все контексты.
20. А еще есть одна существенная штука. Я всей душой ненавижу интерфейсы где ты точно помнишь положение нужной кнопки, заходишь и желаешь тыкнуть, а он еще не прогрузил нужный тебе пункт, и к тому моменту когда элементы наконец прогрузились ты тыкаешь пальцем, а оно за секунду до этого прыгнуло из-за того что подгрузились еще пункты меню и сместили предыдущие, а ты в итоге кликнул вообще не туда и теперь в лучшем случае выходишь обратно и ждешь новой прогрузки, а в худшем - ты только что инициировал какой-то критически важный функционал в админке по типу рестартнуть спейс. Я больше за то чтобы приложение из коробки понимало сколько где кнопок и где они будут размещены, а уж раз так необходимо время на прогрузку, можно на их месте оставлять неактивные плейсхолдеры на те полсекунды пока там не прорисуется пункт меню, зато все всегда будет на местах и никаких прыгающих элементов. Важно это не критичный баг и он один из последних по приоритетности, более того, я готов применять решение только если оно относительно нетяжелое в плане кода и переписывания функционала. Если это будет массивный реворк на 60% всего дерева дизайна, то отложим как нереализованное на будущее.
### Добавлено 15.06.2026
1. Сегодня был чухан, после нашего поздравления мы поменяли вывод. Теперь анонсится причила (был повтор, что неприятно). Из анонса пропала фотка участника, а это имело очень мощный эффект. Нужно вернуть.
- [x] **F-avatar-fix. Фотка пропала из анонса чухана + п.4 «аватарки не
  обновляются».** (2026-06-15) Диагноз: `avatars.py:24` строил file-URL хардкодом
  на `https://api.telegram.org/...` — это мёртвый egress из HF (РКН-блокировка,
  инцидент 11–12.06). Чухан-анонс делал `send_photo(URLInputFile(avatar_url))`
  → aiogram качал байты по direct-URL → timeout → пост молча падал в текстовый
  фолбэк (`chukhan.py:299-301`), фото исчезало. Прод-проверка (Neon): 4/6 юзеров
  с `avatar_url=api.telegram.org/...`. Воркер `BOT_API_SERVER` проксирует и
  `/file/`-скачивание (проверено curl: HTTP 200, байт-в-байт 69166 B). Фикс:
  URL строится через `bot.session.api.file_url(token, path)` → уважает воркер;
  один URL рабочий и для бота из HF, и для `<img>` в мини-аппе. Self-heal:
  `announce_chukhan` ресинкает аватар перед постом (`chukhan.py:245`) → URL
  перезапишется на воркерный на следующем прогоне. +4 теста
  (`test_avatar_url.py`), сьют **389 passed**. **Sync ✅** (avatars.py + тест,
  diff -q пуст). **PUSH ✅** (2026-06-15) GitHub `86a058e..d9c8d6f`,
  HF Space `03c5d54..83bad5e`.
  ⚠️ Остаток п.1/15.06 «повтор причины» — НЕ баг (`weighted_choice` корректен,
  на ретрае роллится новая фраза; в `WeeklyChukhan` нет колонки reason) →
  кластеризуется с п.12 (причина в истории + префикс «Причина:»), разбираем
  в триаже фидбека.

> ⚠️ Удалён блок «инцидент 10–12.06» (бывшие п.21–25 + «добавлено12.06.2026»):
> забитый канал, cloudflare-прокси, миграция на amvera, новость про блокировку
> webhook-ботов в МСК_0. Первопричина — **внешняя РКН-блокировка вебхуков, вне
> нашего контроля**; от нас фиксов не требуется. Требования из той заметки уже
> закрыты в коде: `sock_connect=8` откатан (HOTFIX 2026-06-12 в `dispatcher.py`,
> плоский `total`), а `/api/worm/history` — не поллинг, а запрос по открытию
> попапа `🪱` (`enabled: open`), нагрузки на Neon не создаёт. Антиспам
> планировщика живёт в блоке **G**. Сырые заметки — `git log --grep "GHG8"`.

- [x] **F-media-fix. Медиа-реакции: «реагирует на 100% вне зависимости от
  настроек».** (2026-06-14) Диагноз: режимы `chance`/`wait_then_chance` гнали
  СЕРИЮ из 10 тиков с независимым роллом на каждом — кумулятивно при «шансе
  10–50%» бот срабатывал с вероятностью ~98% (просто растянуто во времени, потому
  и «с задержкой»). Плюс `chance` и `wait_then_chance` вели себя идентично.
  Решение (согласовано с пользователем): **один честный ролл на мем** + разделить
  режимы. `services/media_reactions.py` — удалены `WAIT_TICKS_MIN`/`tick_chance`,
  добавлены `DEFAULT_WAIT_WINDOW_MIN=15`/`WAIT_WINDOW_MIN_BOUNDS=(1,360)`/
  `clamp_wait_window`. `admin_config.py` — два ключа `chance_base_pct`/
  `chance_max_pct` → единый `chance_pct` (default 30) + `wait_window_min`
  (default 15). `handlers/media_reactions.py` — `_reaction_series` →
  `_wait_then_react` (одно ожидание): `chance`=ролл→реакция сразу,
  `wait_then_chance`=ролл→грейс-окно→реакция если люди промолчали. Фронт
  (`api/admin.ts`, `MediaReactionsScreen.tsx`) — одно поле «Шанс реакции» +
  «Грейс-окно, мин» (только для wait_then_chance), новые подписи режимов. Тесты
  переписаны под single-roll (+ проба честности 30%≈30%), весь сьют **385 passed**;
  TS чист; HF-копия 18/18. ⚠️ **Старые ключи прода** `chance_base_pct`/
  `chance_max_pct` больше не читаются → шанс сбросится к дефолту 30% (это и
  желаемо: старые значения вводили в заблуждение). **Sync ✅** (6 файлов,
  diff -q пуст). **PUSH ✅** (2026-06-14) GitHub `eaacb5b..01bfd04` (Pages),
  HF Space `71c52f1..03c5d54`.
- [x] **F1. Переверстать `ProfileScreen`.** (2026-06-18) Закрыто в T1.5: крупная
  центрированная аватарка → сводка лох/чухан → Топы/История → причина в строках
  истории (чухан-причина пришла из T1.2).
- [x] **F2. Приветственный попап: описание + тогл в одну строку.** (2026-06-18)
  Закрыто в T1.3 (`GreetingSettings`, `ProfileScreen.tsx`).
- [x] **F3. «Формат участника» — настройка в одну строку.** (2026-06-18) Закрыто
  в T1.3 (там же, единый блок настроек профиля).
- [ ] **F4.** Тесты (если затронут бэк) + sync + push HF/GitHub.

### G. Антиспам планировщика (прод-фидбек п.14, п.21) — ПРИОРИТЕТ
Контекст (пользователь, 11.06): хрупкий бесплатный сервер + узкий канал
(РКН-throttling). Половина job'ов — поллинг «а не пора ли», хотя событие
редкое или НИКОГДА не наступало в проде. Главное требование: **свести весь
периодический трафик (исходящий к TG + SELECT в Neon) к минимуму; для
оставшихся частых опросов согласовывать частоту с пользователем перед кодом.**
⚠️ Любую смену интервала/частоты НЕ коммитить без явного «ок» от пользователя.

Инвентаризация на 11.06 (по `app/bot/scheduler.py`, проверено):
- `proxy_health` — ✅ ГОТОВО (11.06, не запушено): job регистрируется ТОЛЬКО в
  режиме `ALWAYS_ON`. Было `IntervalTrigger(3600s)` безусловно = 24 холостых
  getMe/сутки; алёрт про мёртвые прокси и так срабатывает лишь в ALWAYS_ON, а
  AUTO_FALLBACK управляется per-request в dispatcher (тик его НЕ питает —
  проверено). В проде режим AUTO_FALLBACK/ALWAYS_OFF → **0 запросов**. Смена
  режима из админки (`PUT /admin/proxy/mode`) дёргает `reschedule_proxy_health`.
  `scheduler.py` (reschedule_proxy_health + перевзвод на старте), `routes_admin.py`
  (PUT). Без юнит-теста (нет async-scheduler+БД стенда; логика тривиальна),
  сьют 376.
- `loser_outbox_retry` — `IntervalTrigger(5мин)`. 288 SELECT/сутки в Neon; по
  словам пользователя ни разу не сработал по назначению. Он вручную сдвинул.
- `bot_pause_auto_restore` — `IntervalTrigger(5мин)`. 288 SELECT/сутки; крутится
  даже когда паузы нет (а это 99% времени).
- `space_restart_tick` — ✅ ГОТОВО (11.06, не запушено): переведён на событийную
  модель. Был `IntervalTrigger(5мин)` = 288 холостых SELECT/сутки → теперь один
  `DateTrigger` на точное время (`compute_next_fire`, учёт 30-мин анти-лупа);
  при `mode=off` job не регистрируется = **0 запросов в простое**. Перевзвод на
  старте + после выстрела (self-rearm) + из admin PUT. `space_restart.py`
  (compute_next_fire), `scheduler.py` (reschedule_space_restart), `routes_admin.py`
  (PUT). +6 тестов, сьют 376.
- `chukhan_retry` — `IntervalTrigger(30мин)`. 48 SELECT/сутки.
- `dead_chat_hourly` — `IntervalTrigger(1ч)`. 24 SELECT/сутки; пользователь:
  «возможно осмысленно, но хочу гораздо реже».
- `birthdays_daily` — `CronTrigger(9:07/сутки)`. 1/сутки. Пользователь спросил
  «зачем каждые 9 часов» — на деле раз в СУТКИ (он прочёл «9» как интервал);
  всё равно ставит вопрос «достаточно и пары раз в неделю».
- `autoloser` — по расписанию; пользователь: «не пролетает в чат когда ловим
  блок за спам» (связано с блоком F-баги/Q-NET, не чисто частота).
- `meeting_reminders_tick` — `IntervalTrigger(tick_minutes)`, кламп 1..120 мин
  (`admin_config.get_reminders_tick_minutes`). П.14: «не могу сделать реже
  120 мин, а по-хорошему раз в ~6 часов».

Принцип решения (обсудить, НЕ кодить без согласования):
- Поллинг ради «не пора ли» с редким/никогда-событием → перевести на
  **событийное one-shot планирование** (DateTrigger на точное время) ИЛИ не
  регистрировать job совсем, пока в очереди пусто. Шаблон: автолох уже умеет
  `DateTrigger` + переустановку в `reload_dynamic_jobs`.
- `proxy_health` при «прокси никогда не работали» — кандидат на отключение
  по умолчанию (env-флаг/admin-toggle), либо резкое урежение.
- `bot_pause_auto_restore`/`space_restart_tick`/`loser_outbox_retry` —
  поднять интервалы и/или включать job только когда есть отложенная работа
  (pending-строка в outbox / активная пауза / включённое расписание рестарта).
- Поднять верхний кламп `reminders.tick_minutes` со 120 до ~360+ (п.14).
- `birthdays_daily` оставить раз в сутки (1 запрос — не спам), но обсудить.

Подход (согласовано 11.06): идём по ОДНОМУ job'у за раз — обсуждаем целевое
поведение, выбираем вариант, кодим, фиксируем, push пачкой в конце блока.
- [x] **G.space_restart** (11.06) — событийная модель, см. строку выше. PUSH ⏳
- [x] **G.proxy_health** (11.06) — job только в ALWAYS_ON, см. строку выше. PUSH ⏳
- [ ] **G.loser_outbox_retry** — обсудить (5мин, 288/сутки, ни разу не сработал).
- [ ] **G.bot_pause_auto_restore** — обсудить (5мин, крутится без активной паузы).
- [ ] **G.chukhan_retry** — обсудить (30мин).
- [ ] **G.dead_chat** — обсудить (1ч; пользователь хочет гораздо реже).
- [ ] **G.birthdays** — обсудить (раз в сутки 9:07; «пары раз в неделю хватит»).
- [ ] **G.meeting_reminders_tick** — обсудить (кламп 1..120, п.14 хочет ~6ч).
- [ ] **G.PUSH** — пачкой: sync `backend/` → `meetup-planner-backend/`, diff,
  коммит, push HF + GitHub (если фронт затронут).

### Q-NET. Сетевая отказоустойчивость (Q8/Q9/Q10) — архитектура
Контекст: РКН душит прокси, всё идёт через direct; джобы зависают, помогает
свап proxy↔direct или смена VPN на телефоне; рестарт Space «расклинивает».
НЕ трогать первый (зелёный) прокси (Q8). Кнопка «найти живые прокси» никогда
не работала — низкий приоритет (Q8).

> **Статус (2026-06-12):** Q-NET.b реализован и задеплоен (см. архив GHG7).
> Остаётся a; c — рекомендация отклонить. **d (переключатель Webhook↔Polling)
> свёрнут** как порождение инцидента 10–12.06: вебхуки лежат из-за внешней
> РКН-блокировки, переключатель транспорта её не лечит. Кодить a только после
> согласования с пользователем.

#### Q-NET-INV: план (2026-06-06, по коду dispatcher.py/proxies.py)

**Диагноз «зависает, рестарт расклинивает»:** aiohttp `TCPConnector` держит
keep-alive-пул; при throttling РКН соединение полудохнет, но остаётся в пуле и
переиспользуется → каждый следующий запрос виснет на нём до 30с. Рестарт Space
= новый коннектор = «расклинило». Свап proxy↔direct помогает по той же причине:
`_swap_session` пересоздаёт `ClientSession` (dispatcher.py:101). ⇒ ядро решения —
**уметь пересоздавать сессию без рестарта и не дать дохлым keep-alive жить**
(базу под это уже заложил Q-NET.b — см. архив).

- **(a) Circuit Breaker** — модуль `app/services/net_breaker.py`: счётчик
  последовательных сетевых фейлов в `make_request` (там уже ловятся
  ClientConnectorError/ClientOSError/ServerDisconnected/Timeout,
  dispatcher.py:167). N фейлов подряд (дефолт 3, admin_config) → «трип»:
  принудительный `_swap_session(текущий transport)` — пересборка ClientSession
  с тем же путём (направление НЕ меняем — Q8), + лог + admin-алёрт
  (rate-limit как у proxy down). Half-open: после успеха счётчик в 0.
  Состояние — module-level (как `_state` в proxies). Без новых зависимостей.
- **(c) curl_cffi** — РЕКОМЕНДАЦИЯ: отклонить. aiogram завязан на
  aiohttp-сессию; подмена транспорта = форк-обёртка над всем Bot API клиентом,
  поломает smart-proxy и (a)/(b). TLS-fingerprint для api.telegram.org —
  гипотеза, факты логов на неё не указывают.
- **(d) Webhook ↔ Long Polling** — ❌ СВЁРНУТ (12.06). Возник в рамках инцидента
  10–12.06 как обход блокировки вебхуков; первопричина внешняя (РКН), смена
  транспорта её не решает. Если дойдёт до миграции/переезда — поднять заново
  отдельным блоком, не тащить в Q-NET.

Рекомендуемый порядок: **только a** (b сделан; c — отклонить; d — свёрнут).

- [ ] **Q-NET.a (10а).** Circuit Breaker / авто-fallback (план (a) выше).
- [ ] **Q-NET.c (10б).** TLS fingerprint / curl_cffi — INV-оценка, реализация
  под вопросом (рекомендация — отклонить, см. (c)).
- [ ] **Q-NET.e.** Тесты + sync + DEPLOY_NOTES для a (для .b уже сделано).

### P6.1.b. Сидинг 6 персоналий (ручной шаг пользователя)
- [ ] **P6.1.b.** Сидинг 6 персоналий (GHG7.txt стр. 154–159) — руками
  пользователя через админку («🎭 Персоналии», формат [шаблоны]/[слоты]).
  Тексты живут в Neon, не в git. Остальное по P6 закрыто (см. архив GHG7).

## Отложено явно (НЕ в этой итерации)
- **Бот сам постит мемы.** GHG7.txt стр. 203–204 — «Пока не реализуем».
- **Реальный цитатор** (P6.4 из GHG7) — требует индекса сообщений в Neon.

###
Добавлено 13.06.2026
1. Придумал гениальную тему, которую крайне просто реализовать. Команда /advice или же хештег #advice или #совет упомянутый в сообщении - бот дает рандомный совет аналогично магическому шару. Запили туда порядка 10-20 разнообразных вариантов в нашем стиле от дефолтных "Да, нет, точно, возможно, не факт" до приколюшечных но чтобы они попадали в смысл. Так добавим возможность включать эту функцию и редактировать фразы из админки. Рандом честный, без счетчиков использования.
Альтернативный триггер: юзер может задать любой вопрос "А не пора ли встретиться, что скажешь? @gunghoguns_bot" либо "@gunghoguns_bot а не лох ли ты часом?" если в сообщении в любом месте упоминается (не важно до или после вопросительного знака) @gunghoguns_bot и оно заканчивается на вопросительный знак, это однозначный триггер на то что нужно ответить на сообщение любым из вариантов как при /advice . Важно чтобы бот понимал разницу где просто цитирование, если вопросительного знака в конце нет, то сценарий такой как раньше был при отметке бота\реплае.
2. На отдаленное будущее можно замутить такую схему что червь-пидор это не просто плашка-статус, оно еще и символизирует что ты как бы становишься на этот период хозяином бота и он твой прислужник. А выражаться это будет в следующем:
а) Если юзеру который в данный момент является червем, выпал неудачный статус типа как лох или чухан, то все анонсы работают как и раньше, но при этом бот выражает свое соболезнование, в стиле: 
Лох дня - Серж, причина: блабла 
Но при этом бот добавляет подхалимский префикс в духе "С прискорбием вынужден сообщить" "Новость печальная, но тем не менее" "Мой Господин %username$, вынужден вас расстроить" либо суффикс после "Да, новость не из приятных" "Я тоже с этим глубоко не согласен, милорд" "Вопиющая несправедливость" "Ваша неудача и мое личное поражение, милорд %username%" и т.д. Нужен набор от 5 до 10 подхалимских префиксов и суффиксов с возможностью редактирования (можно смело делать отдельный блок настроек под червя с редактированием шанса и принудительным назначением роли юзеру с анонсом в чат и без). Нужно чтобы фразы чередовались более-менее без частых повторов, можно сюда подбить механику счетчика использования.
б) Режим поддакивания. На любое сообщение господина (нужны варианты настройки, рандом например на каждые 3-5 сообщений, или каждое сообщение крутит шанс на триггер поддакивания, или на тайм интервал - поддакивание не чаще раза в 1 час или 5 часов - всё настраиваемо в блоке настройки червя). Само поддакивание проявляется так:
Юзер общается в чате, бот выбирает в зависимости от условий сообщение господина и цитируя его - комментирует в положительном ключе "Удваиваю!" "Истина." "Каждая фраза - как молитва!" "Господин %username% не может ошибаться." "Документально подтверждено." и тд. тоже с запасом в 5-10 фраз редактируемых. Нужно чтобы фразы чередовались более-менее без частых повторов, можно сюда подбить механику счетчика использования. Режим поддакивания отдельно отключаем в админке, так и самим пользователем-червем через команду /отвали (или типа того). Периодически, примерно на каждые 5-10 поддакиваний бот может напоминать "Если утомляю вас навязчивостью, милорд %username% - можете использовать команду /Отвали"
в) Возможность наказать кого либо в словесной перепалке, натравив на него бота. Только участник со статусом червя может использовать команду /punish @user либо же #punish @user либо #наказать @user (не уверен что все это можно реализовать прямо в таком виде, но суть ты понял) и бот выкатит анонс в стиле текстовых рпг о том что произошло, например я выбрал /Punish @серж и бот выкатывает "Влетаю с двух ног в грудину @серж" или "Выливаю ушат говна с высокой башни на голову @серж" или "Плеснул банку застоявшейся мочи прямиком на рабочий стол говноеду @серж" - тоже запас порядка 20 фраз с счетчиками и редактором в админке.
г) При анонсе того что кто-то стал червем, помимо упоминания о том что это невероятная удача и супер редкая фишка с шансом (указать текущий шанс, в том числе если он был изменен в настройках), нужно так же максимально кратко но доступно описать, что это за собой повлечет. Что-то в стиле: "С данной минуты, %username% - мой великий господин. Я буду стараться во всем ему угождать и поддерживать и подчиняюсь прежде всего его воле. А если недруги застанут вас в расплох, то расквитаюсь с ними я по команде /punish @user"

### Добавлено 16.06.2026
1. Идея музыкальной предложки. В основном это только идея, конкретного видения реализации нет, поэтому нужна твоя помощь. Но суть такая что в течение недели участники могут накидывать боту в личку музыкальные произведения (в идеале прямо вайлом мп3 внутри телеги. Либо ссылкой на ютуб, но он работает только с впн, по этому не знаю какое будет взаимодействие у бота). Максимальное количество треков в подборку от одного участника будет настраиваемым, но по умолчанию до 5. Бот в свою очередь ведет еженедельный пул подборок и в сценарии где каждый пользователь скинул по одному треку, в назначенный день дня недели (настраиваемо, условно вторник - 12 часов) - бот выплевывает подборку 4-10 (настраиваемо) треков с каким-то дежурным бодрящим сообщением которое как нахваливает выбор участников, так и скептично его оценивает или вообще в афиге что это за кал и кто это сюда накидал. У бота может быть память какие треки уже использованы, а какие можно придержать для следующей подборки. В идеале при оформлении поста иметь настройку а) треки просто скидываются одним постом-пачкой б) в пачке каждый трек подписан типа кем предложено и когда. При реализации данной фичи нужно опираться на то что у нас слабый хост и мы точно не можем себе позволить хранить физически файлы в памяти, только какие-то отличительные данные или теги. В идеале конечно было бы неплохо сразу в админке посмотреть уже формирующийся пул предложки на ближайшую неделю, даже что-то удалить\добавить ручным редактированием. Чтобы все пердыдущие подборки сохранялись и можно было бы навигировать как по кнопкам-мини плейлистам (но опять же, только если не будет жрать память) а вообще идеальнее было бы при публикации подборки просто сохранять ссылку на пост чтобы из админки можно было увидеть лист "подборка-дата" и кликом тебя перенаправило бы на пост, а сам бот не хранит в памяти лишней инфы и сложных структур.
Если в подборке меньше 4 (настраиваемо) треков на момент публикации, бот выкидывает анонс что не накопил достаточно треков, но вы можете ему с этим помочь. Обычно в этот же момент юзеры накидают с запасом, поэтому задача бота не сразу, а продублировать попытку публикации в следующие 3-6 часов. По опыту предыдущих фич - не вздумай делать более частые проверки которые могут забить канал.
2. Мьюзик-гейм на основе предыдущего поста. По запросу какой-то определенной командой, бот выкидывает в чат рандомный трек из предложки и предлагает выбором участников угадать, кто это предложил. Есть настройка для того чтобы эта голосовалка выпрыгивала автоматом (один раз каждый день недели, любой день можно отключить), по умолчанию автоматический вызов выключен.
3. На будущее можно еще нараубить задел под систему оценки подборок по количеству лайков, но в целом это не особо нужная функция.

### Добавлено 17.06.2026
1. Я был топ 1 по лохам, но сегодня кравченко меня обогнал. Было бы круто если бы бот анонсировал когда кто-то новый берет топ1 по лохам\чуханам. Типа был анонс лоха, челу закидывает +1 и он со второго места становится первым, бот как обычно анонсирует Лоха дня и добавляет фразу типа "И %username% становится новым лидером" или "Забирает перстень первенства в этой номинации" "Берет лидерство в гонке лохов" и тд. Дополнительно к этому выводит текущий топ командой /top. Сама вункция анонса топа - выключаема, фразы - редактируемы. (импортер фраз обновить чтобы и отсюда тоже забирал). прикреплять дополнительно выноску топ или нет - тоже отдельный тогл (по умолчанию вкл)

### добавлено 18.06.2026
1. Я в целом доволен функцией реакта на мемы, бот очень смешно попадает в контекст и не задалбывает слишком частыми реакциями. Но возникло некое несоответствие. Из-за того что бот реагирует практически моментально на любое медиа, как он будет понимать что была подборка с учетом того что подборки обычно выкладываются поэтапно, в течение какого-то промежутка. Не нужно рубить с плеча, возможно у нас уже реализованы альтернативные режимы которые позволяют покрыть оба сценария. Просто любопытно как устроена логика именно текущего набора настроек 'с шансом' '60%' "Случайно, одно из двух"
2. Я только позавчера делал запрос аватарок, при том что один участник реально на днях обновил себе пикчу в телеге. Оно работает. Но почему приложуха забывает аватарки по прошествии некоторого времени? Сейчас снова вопросы у всех кроме юзеров серж и митян, при том это два единтсвенных участника у кого мы не могли получать аватарки (настройки приватности) и я вместо них тупо подставил вручную левые картинки (но смешные) в бд. Нужно разобраться, чтобы бот получив единожды аватарку пользователя не забывал ее до следующего обновления. Можно запилить отдельное меню аватарок где будут настройки\планировка синхронизации аватарок и там же можно вручную подставить ссылкой для каждого участника отдельно какую-нибудь особую смешную аватарку. Тем более что в будущем у участников будет запилена функция ручного подставления аватарки (она как бы значится чисто внутри приложения, для вывода юзеров в таблице и при анонсах (чухана) и перекрывает существующую тг аватарку пользователя). Но при принудительном запросе синхронизации, подставляется именно тг-шная аватарка. Исключение - если не удалось получить, тогда используется ранее полученная\загруженная вручную.
- [x] **AV-18.06 (18.06 #2). Аватарки протухали → стабильный file_id.**
  (2026-06-18, залогировано 23.06) Корень: `users.avatar_url` хранил TG file-URL
  с `file_path` (живёт ≥1ч → 404 → `<img>` в мини-аппе ломался; «приложуха
  забывает аватарки»). Серж/Митян не протухали — у них вручную вписаны ВНЕШНИЕ
  ссылки. Фикс (миграция `0017_avatar_stable`, 3 nullable-колонки): хранимый
  стабильный `avatar_file_id` (не протухает) + прокси-роут `/api/avatar/{id}?v=`
  (cache-buster = хвост file_id; смена фото меняет file_id → меняется `?v=` →
  свежий кадр) резолвит file_path на лету; `avatar_manual_url` (ручная
  картинка-ссылка, ПЕРЕКРЫВАЕТ TG для отображения, пусто → сброс к TG);
  `avatar_synced_at` (для меню «когда синкали»). ⚠️ frozen `avatar_url` НЕ
  тронут (его читает чухан-постинг, синкает прямо перед `send_photo`). Админка:
  `GET /admin/avatars/list` + `PUT /admin/avatars/{id}/manual`, фронт
  `ManualAvatars` в `ScheduledPublicationsScreen` (список участников + поле
  ручной ссылки, оптимистичный апдейт). `ProfileScreen`/`/users/me` рисуют
  через прокси-URL. +12 тестов (`test_avatar_url.py`), сьют **477 passed**, tsc
  + vite build чисты. **Sync ✅** (avatars/models/routes_admin/routes_users +
  миграция + тест, diff -q пуст по этим файлам). **PUSH ✅** HF
  `767586c..5700d5f` (вместе с T3.6.1–5), GitHub Pages `1f9fe3b..490505a`.

### добавлено 19.06.2026 
1. Открыл с компа и офигел, дошло до смешного, у нас есть окно приветствия, вот эта ненужная плашка "Не размечен день = считается занятым. Тапай по дате, чтобы открыть редактор." так еще и 4 кнопки внизу "опрос авто-лох-прогон фразы и тд"
2. Будет круто сделать кнопку приветствия схлопываемой, если нажать по ней то она превращается в одну строчку с индикатором что это разворачиваемый элемент (плоская стрелка вниз). Функция закрыть так же остается интерактивной. из-за того что сейчас вывод трех пунктов выглядит несимметрично, можно добавить 4-ый (чухан недели + лох дня, главный лох, главный чухан). Не забыть что у нас вроде как где-то размечен вывод червя на случай если таковой есть. По умолчанию - развернута. Аналогично с четырьмя кнопками, можно схлопнуть и они превратятся в строку со стрелкой вверх и пометкой что тут кнопки.
3. Когда кто-то цитирует бота и он на это отвечает шизоцитатой, в 99% получается полная ахинея. Теперь режим ответ/реплай имеет собственный пул фраз, например:
Так точно 🫡 
Ну так, епт
Да конечно, хуле там
Заранее извиняюсь 
Вы все правильно поняли 
Я это и хотел сказать 
Не пали контору 
Тссс…
Ну все все успокойся 
Да не ной бля

Функционал снапшота надо обновить чтобы он был в курсе нового пула фраз и его тоже учитывал. И вообще возьми за правило всегда по добавлялении/изменении механик пула фраз актуализировать и состояние снапшот механики
### Добавлено 22.06.2026
1. После того как выбран чухан и запускается голосовалка на оспаривание, таймер в один час или сколько-то там это очень мало, никто еще не успел толком проголосовать. Увеличить дефолтное значение и сделать этот таймер настраиваемым в соответствующем подменю относящемся к чухану.
2. Прервался наш самый долгий стрик бесперебойной работы бота за все время. Сегодня в 10 утра опубликовался чухан, после этого не отправилась в чат ни одна задача по отправке шизоцитаты или анонса лоха. К сожалению я не сохранил лог и просто рестартнул вручную HF space, после чего вручную задал новый таймер лоха, но он снова не отправился два из двух раз. Я не знаю с чем это связывать, поскольку до этого была жесткая установка продолжать работу над обновлениями по чеклисту и обходить коннект с неоном любой ценой, а в крайнюю ночь\утро я дал отмашку закрывать все оставшиеся части чеклиста и тут же мы словили новый блок. Либо что-то тригернул последний чухан, поскольку он корректно отправился. Либо вообще нет никакой связи между текущим функционалом бота и проблемы опять не на нашей стороне.
3. В телеграм пабликах сегодня проскакивала новость что множество сервисов испытывают проблемы из-за cloudflare. Так же мне ну удалось зайти на https://console.neon.tech/ используя 4 разных впн-а.